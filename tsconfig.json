{
  "compilerOptions": {
    /* Language and Environment */
    "target": "es2016",                                  /* Compatible with modern Node.js */
    "module": "CommonJS",                                /* Matches Node.js module system */

    /* Paths */
    "rootDir": "./src",                                  /* Source files are in src/ */
    "outDir": "./dist",                                  /* Output compiled files to dist/ */
    "esModuleInterop": true,                             /* Better CommonJS interop */
    "forceConsistentCasingInFileNames": true,            /* Ensure consistent file names */

    /* Type Checking */
    "strict": true,                                      /* Enable strict type checking */
    "skipLibCheck": true                                 /* Skip type checking of declaration files for speed */

    /* Emit */
    // "sourceMap": false,                               /* Omit for production to reduce size (add if debugging needed) */
    // "declaration": false,                             /* Omit .d.ts files unless needed for external use */
    // "removeComments": true                            /* Remove comments for smaller output (optional) */
  },
  "include": ["src/**/*"],                               /* Include all TypeScript files in src/ */
  "exclude": ["node_modules", "dist"]                    /* Exclude node_modules and dist */
}